# Secret key used to sign and verify JWT tokens.
JWT_SECRET=change-it-to-long-random-secret


# Assignment requires 8080 for local Docker runs.
# Some free hosting platforms inject their own PORT
PORT=8080
